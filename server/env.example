# SMTP credentials (use your provider's settings)
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Where to send incoming contact messages
INFO_TO=info@gonow.dk

# From header used when sending from server
FROM_EMAIL=no-reply@gonow.dk
FROM_NAME=NOW Website

# CORS (optional): set your frontend origin if hosted on another domain
# CORS_ORIGIN=https://gonow.dk

# Server port (optional)
# PORT=3001
